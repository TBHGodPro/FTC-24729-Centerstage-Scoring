import { s } from './CustomStylesType';

export default {
  textAlign: s({
    left: {
      textAlign: 'left',
    },
    center: {
      textAlign: 'center',
    },
    right: {
      textAlign: 'right',
    },
  }),
};
